package Arrays;

public class reversesArray {
    public static void main(String[] args){

        // Make the Array to print elements from back to beginning

        String[] cars = {"Lexus", "Toyota", "BMW", "Ford"};

    }
}













/*
public static String reversesArray (String [] arr) {
        int size = arr.length;
        StringBuilder result = new StringBuilder();
        for (int i = size - 1; i > 0; i--) {
            result = result.append(arr[i]).append(" -> ");
        }
        return result.toString();
    }
 */